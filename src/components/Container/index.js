const Container = ({children}) => {
    return (
        <main>
            <h1 className='text-red-300 text-3xl font-bold underline'>test</h1>
            {children}
        </main>
    )
}

export default Container